class MazePath {
  constructor(tree) {
    this.tree = tree;
    this.startNode = this.tree[0].source;
  }

  generatePath(tree) {
    for (let i = 0; i < tree.length; i++) {}
  }
}

export default MazePath;
